# ===== 云部署配置模板 =====
# 复制本文件为 .env，填入实际值后使用

# Flask
SECRET_KEY=改成一个随机字符串
FLASK_ENV=production

# 存储方式: local（本地磁盘）、oss（阿里云OSS）、bos（百度云BOS）
STORAGE_TYPE=oss

# 对象存储配置（阿里云 OSS 或 百度云 BOS 均填这四项）
OSS_AK=你的AccessKeyId
OSS_SK=你的AccessKeySecret
OSS_ENDPOINT=https://oss-cn-hangzhou.aliyuncs.com
OSS_BUCKET=你的bucket名称

# 数据库（SQLite 文件路径）
DATABASE_PATH=/opt/demo-music/backend/data/music.db

# 音乐文件目录（STORAGE_TYPE=local 时使用）
MUSIC_DIR=/opt/demo-music/music
